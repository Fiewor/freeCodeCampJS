const shortestPath = (path) => {
  let graph = {};
  graph["start"] = {};
  for (let path of paths) {
    let start = 0;
    if (path != start) {
    }
  }
};

// let paths = [[1, 5], [10, 3], [3, 4]]
